[package]
name = "gateway"
version.workspace = true
edition = "2021"

[dependencies]
# Shared types (single source of truth)
hypermesh-lib = { workspace = true }

# STOQ transport (for STOQ bridge)
stoq = { path = "../stoq" }

# TrustChain — FALCON-1024 node identity (NodeSigner) + TrustChainProofProvider
# (StateProofProvider) for the full-STOQ-PoS gateway mode (F8). Depends only on
# stoq/lib, so no dependency cycle (trustchain does not depend on gateway).
trustchain = { path = "../trustchain" }

# Engauge for SSE metrics streaming (MetricsPublisher + SseFrameBridge)
engauge = { path = "../engauge" }

tokio = { workspace = true }
h3 = "0.0.8"
h3-quinn = "0.0.10"
quinn = { workspace = true }
rustls = { workspace = true }
rustls-pemfile = { workspace = true }
rustls-native-certs = "0.8"
rcgen = { workspace = true }
http = "1.1"
bytes = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
anyhow = { workspace = true }
toml = { workspace = true }
dashmap = { workspace = true }
arc-swap = { workspace = true }
futures = { workspace = true }
pin-project = "1.1"
thiserror = "2.0"
async-trait = { workspace = true }
chrono = { workspace = true }
blake3 = { workspace = true }
pqcrypto-falcon = { workspace = true }
pqcrypto-traits = { workspace = true }
rand = { workspace = true }
hex = { workspace = true }

[dev-dependencies]
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
tower = "0.5"
hyper = "1.5"
tempfile = { workspace = true }

[[bin]]
name = "gateway"
path = "src/main.rs"

[[bin]]
name = "test-week1-endpoints"
path = "src/bin/test_week1_endpoints.rs"

[lints]
workspace = true
