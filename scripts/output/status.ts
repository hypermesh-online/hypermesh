// AUTO-GENERATED by scripts/sync-status.ts
// DO NOT EDIT - update crate-status.toml files instead

export interface CrateStatus {
  id: string;
  name: string;
  description: string;
  phase: "planning" | "alpha" | "beta" | "stable";
  features: {
    working: string[];
    inDevelopment: string[];
    planned: string[];
  };
  completion: number;
}

export const crateStatuses: CrateStatus[] = [
  {
    "id": "base",
    "name": "Substrate",
    "description": "Sovereign addressing and link/carrier management beneath the kernel",
    "phase": "alpha",
    "features": {
      "working": [
        "Sovereign address derivation — NodeId → matrix cell + fd48:4d00::/32 (derive_cell + derive_address, one canonical BLAKE3 construction; blockmatrix MatrixCoordinate::derive_cell delegates here)",
        "Address verifiability — address is a pure function of NodeId, any peer recomputes it (no DHCP, no lease); unit-tested byte-identical to the legacy blockmatrix cell construction",
        "sysfs interface enumeration + carrier-state reading (read-only, default-on degraded tier, R16)",
        "DefaultSubstrate::active_interface() — carrier→up→non-loopback→loopback selection (R16 graceful degradation)",
        "SubstrateAdapter registry — capability-tier selection (netlink → sysfs → fallback), best available backend chosen at runtime",
        "Node binary integration — node constructs base::DefaultSubstrate and injects active_interface() into STOQ TransportConfig; base has no upward dep, STOQ does not depend on base"
      ],
      "inDevelopment": [
        "rtnetlink backend (Substrate.b) — real netlink enumerate/carrier/assign_address, verified musl-clean, but feature-gated `rtnetlink-backend` (OFF by default)",
        "Lease-free address assignment — implemented in the rtnetlink backend only (feature-off); sysfs tier returns Unsupported"
      ],
      "planned": [
        "Substrate.b — link-flap self-healing + QUIC connection-migration signal (watch_links currently returns an empty stream)",
        "Substrate.a — reachability discovery via STOQ reflector extension (detect_reachability returns Unsupported; reachability() falls back to Unknown)",
        "radio_mesh backend — Substrate.c device-to-device (R&D stub, returns Unsupported)",
        "windows backend — future cross-platform (stub, returns Unsupported)"
      ]
    },
    "completion": 50
  },
  {
    "id": "block-matrix",
    "name": "Block-MATRIX",
    "description": "3D mesh topology, matrix-aware distributed computing, and asset pipeline",
    "phase": "alpha",
    "features": {
      "working": [
        "3D coordinate system (x,y,z positioning)",
        "Tensor math library (Vector3D, Matrix3x3, A*)",
        "Geospatial module (GPS conversion, clustering)",
        "Every-node blockchain (independent Device chain, genesis on boot)",
        "Blockchain state persistence (WAL, snapshots, recovery via PersistenceManager, versioned block format v1 with BLAKE3 integrity verification on every read) — until S3.0 the machinery existed but RUNTIME blocks never reached it (save_block had no call site in mutations.rs), so every stored asset was lost on restart",
        "S3.0 durable block write-through — BlockSink at the insert_block chokepoint persists every block (genesis and runtime), fail-closed: persist before in-memory publish, so a failed write cannot be silently accepted",
        "S3.0 blocking I/O off the async path — fsync/WAL/metadata/index writes moved to spawn_blocking (previously ran on a tokio worker inside four held chain write locks, once per block)",
        "S3.0 add_block head→insert race fixed via head reservation — concurrent writers went 2/8 → 8/8 with no silent loss",
        "S3.0 deterministic genesis — four clock reads removed from the genesis path so peers can reproduce and adopt it; live runtime proofs keep real clocks + freshness nonces",
        "S3.0 genesis adoption transport — genesis request/response carries a real verified Block instead of a hash string, recorded non-destructively per network (destructive adopt_genesis deliberately not wired — S3.4)",
        "NodeBlockchain::from_blocks() chain reconstruction with integrity validation",
        "Node resume path (load persisted genesis/blocks/cert from disk)",
        "BLAKE3 content hashing for blocks",
        "Asset pipeline: Compress(Brotli)->Encrypt(Kyber-1024 KEM)->Shard(ngauge/NGauge Sharder, RS 10+4)->store locally",
        "Sharding authority is NGauge (A1) — blockmatrix pipeline CONSUMES ngauge::Sharder/ShardingConfig; Shard/ShardMetadata/ShardingStats live in hypermesh-lib (no dependency cycle)",
        "Streaming-first asset pipeline — segment-oriented processing with per-segment compress/encrypt/shard (R13)",
        "Zstd compression with content-type auto-detection (video/audio skipped, text->Brotli, binary->Zstd)",
        "Per-segment Kyber-1024 KEM encryption — one KEM per asset, BLAKE3 HKDF per-segment AES-256-GCM keys",
        "Random-access byte range reconstruction — reconstruct only segments covering requested range",
        "Async streaming I/O — process_stream (AsyncRead) and reconstruct_to_writer (AsyncWrite) for bounded-memory pipelines",
        "Adaptive erasure coding (R14 — RS parameters scale with asset size), engine now provided by ngauge::Sharder",
        "Store command: file -> pipeline -> local shard files + shard map",
        "Fetch command: local shard files -> reverse pipeline -> reconstructed file",
        "Asset adapters (CPU/GPU/Memory/Storage/Network/Container) — struct definitions and local operations",
        "AssetRegistration with BLAKE3 content hash (stored in blocks as hash only, not full data)",
        "IPv6 asset addressing (AssetAddress: fd48:4d00 prefix, matrix coords, content fingerprint)",
        "Privacy allocation (PrivacyMode: Anonymous/Private/Public — transport layer selection)",
        "Privacy-aware certificate strategy (NetworkType selection based on PrivacyMode)",
        "Node bootstrap Anonymous/Private/Public certificate routing",
        "Node bootstrap lifecycle (genesis block, self-signed cert, DNS init)",
        "Internal DNS service registration (5 services at boot, resolve_service())",
        "DNS CLI (register/resolve/list subcommands)",
        "DNS persistence across restarts (dns_records.json)",
        "DNS-as-asset blockchain registration (proper AssetRegistration with BaseSystemType::Dns)",
        "CLI for matrix topology queries, node management, and asset operations",
        "Cross-platform OsAbstraction (Linux/macOS/BSD/Windows, hardware detection)",
        "Real hardware metrics collection (CPU/memory/network/storage from /proc)",
        "Capability-based GPU detection (honest fallback, no fake hardware)",
        "StateAuthenticator trait (binary authentication interface)",
        "PeerIdentity bridges to lib NodeId",
        "Proof of State validation on block creation — add_block() requires Vec<BlockAssetEntry>, each entry carries its own StateProof",
        "Per-entry proof hash — hπ = BLAKE3(StateProof) in every BlockAssetEntry, validated independently",
        "Canonical block formula — Block_i = { prev_hash, entries: [{ hA, hπ, state_proof, ptr }] }, no timestamp/coordinate/nonce",
        "Module split — chain.rs, mutations.rs, genesis_ops.rs, stoq_transport.rs (all <500L)",
        "Scope-aware block sync — Device (independent chains), Network/Private (shared chain full replication)",
        "SpatialBucketAssigner — adaptive neighborhood radius via sqrt(n) k-nearest peers, octree-like partitioning of 3D hash volume",
        "Genesis hardware assessment — OsAbstraction detects CPU/Memory/Storage/Network/GPU, registers as assets in block #1",
        "JSON-RPC 2.0 IPC over Unix domain sockets (protocol, server, client, handler)",
        "Daemon lifecycle (connect/disconnect commands, PID file, graceful shutdown)",
        "IPC handler dispatch (ping, shutdown, status methods)",
        "DaemonState shared context for IPC handlers",
        "Socket path 3-tier fallback ($HYPERMESH_SOCK > $XDG_RUNTIME_DIR > ~/.hypermesh)",
        "IPC handler modules (dns, blockchain, network, topology, asset, config)",
        "All CLI commands route through IPC when daemon is running",
        "Standalone fallback for store/fetch without daemon",
        "CliOutput Serialize support for JSON IPC responses",
        "TOML config file (~/.hypermesh/config.toml) with load/save/merge",
        "Config CLI (show/get/set/init subcommands)",
        "--json global flag for machine-parseable output",
        "--config global flag for custom config path",
        "CLI flags override config file values (CLI > config > defaults)",
        "Domain-as-network — registering a domain creates a Network-scope blockchain (BLAKE3 chain ID)",
        "DomainRegistration struct with parent chain derivation and persistence",
        "Hierarchical DNS resolution — walks parent domain chains right-to-left",
        "DomainNetworkManager — bridges domain names to SyncManager network join/leave",
        "Domain invitation tokens — BLAKE3-HMAC keyed by owner proof, time-bounded",
        "Domain CLI (register, create, list, nodes, invite subcommands)",
        "Join command — join a domain network with optional invitation token",
        "Domain IPC handlers (domain.register, domain.list, domain.join)",
        "Dashboard manifest (dashboard.toml) with parse/validate, scope-aware access (public/private)",
        "Dashboard CLI (deploy, list, info, init/scaffold)",
        "Dashboard scaffold generator — creates project structure with dashboard.toml and scope dirs",
        "Dashboard asset type (BaseSystemType::Dashboard, AssetType::Dashboard) — first-class blockchain asset",
        "Dashboard deploy pipeline — bundle files → BLAKE3 content hash → register on blockchain → store in asset store",
        "Dashboard IPC deploy — registers Dashboard asset on blockchain, stores bundle keyed by content hash",
        "Dashboard IPC list/info — queries blockchain for Dashboard-type assets (chain is source of truth)",
        "Default system dashboard auto-registered as blockchain asset on first boot (public/private scopes)",
        "STOQ API bridge — all IPC handler methods (20+) exposed over STOQ protocol via IpcBridgeHandler adapter",
        "TypeScript SDK (@hypermesh/sdk) — typed client wrapping Gateway /api/v1/* endpoints",
        "Python SDK (hypermesh) — sync (urllib) + async (httpx) client wrapping Gateway API",
        "C# SDK (HyperMesh.Sdk) — .NET 8.0 async client wrapping Gateway API",
        "C++ SDK (hypermesh_sdk) — C++17 POSIX socket client wrapping Gateway API",
        "Go SDK (sdk-go) — zero-dep client with context.Context wrapping Gateway API",
        "SDK service modules — per-service base URLs/ports (Caesar:9294, TrustChain:8444, NGauge:9296, Catalog:9295)",
        "SDK examples and GUIDE.md for all 5 languages (basic, blockchain, dns, domain, dashboard)",
        "CLI service subcommands (hypermesh caesar/trustchain/ngauge/catalog) with IPC relay",
        "CLI --mode flag (auto/ffi/ipc/http) and per-service URL flags",
        "Config [services] section (ConnectionMode, per-service URLs in config.toml)",
        "Identity-as-asset — FALCON-1024 + Kyber-1024 keypair registered as SystemAssetKind::Identity blockchain asset in genesis block (R1/R10)",
        "Bilateral handshake wiring — connect_to_peer()/accept_connections() delegate to stoq bilateral handshake via NodeSigner+StateProofProvider traits, tested two-node local and local-to-GCP",
        "DNS propagation via blockchain — DnsBlockEntry serialized into BlockAssetEntry with BaseSystemType::Dns, extracted on receive, re-populated on restart",
        "CA certificate enrollment — after bilateral PoS handshake, TrustChain CA issues cert to replace self-signed bootstrap cert (§5.7 Phase 2)",
        "Stream type discrimination — CONN_TYPE_HANDSHAKE (0x00) and CONN_TYPE_PEER_MESSAGE (0x01) on every STOQ connection",
        "Genesis Proof of State from real hardware — OsAbstraction-assessed capabilities bind the four proofs (S1 removed the (cores*mhz)+memory_mb stake formula and the minimum_stake gate; capacity is descriptive, PoStake is an identity binding)",
        "Recovery passphrase commitment in genesis block Identity asset — HKDF-SHA512 + BLAKE3 (§6.2.3)",
        "Block fetching protocol — TransportSyncDriver pulls missing blocks from reflectors via SyncRequest→SyncResponse→BlockFetchRequest→BlockFetchResponse over STOQ streams",
        "Metrics emission to ngauge — MetricsReporter pushes Capacity + Congestion + Routing frames (eBPF-sourced) via STOQ streams (CONN_TYPE_METRICS=0x02) to peers every 30s with backoff",
        "Real FALCON-1024 signed state proofs — re-exports TrustChainProofProvider (WireSignedProof envelope with BLAKE3+FALCON signing/verification)",
        "Network sync MVP — shared --network-id CLI, runtime block propagation (5s poll), cross-genesis insertion, e2e verified two-node over QUIC",
        "Block sync wire protocol — TAG_BLOCK_ANNOUNCE format, BLAKE3 hash verification, insert_received_block with cross-genesis tolerance",
        "PoS-gated shard access control — AuthenticatedPeer tracking, verify_peer_access() gates shard/sync/block-announce/transfer/CA messages, network-ID scoping, per-entry proof_hash validation (P1/F1: TAG_BLOCK_ANNOUNCE now in the needs_auth allowlist — a mirror is only accepted from a bilaterally-PoS-authenticated peer)",
        "Shard distribution to network peers — placement-aware routing (closest peer by matrix coordinate), only to authenticated peers, graceful degradation to local storage",
        "IPC store handler — full pipeline (Compress→Encrypt→Shard→Distribute) with network distribution via shard transport",
        "ShardStore disk persistence — shards persisted to disk, loaded on restart, survives node restart",
        "Auth TTL — authenticated peers expire after 1 hour, re-authentication required",
        "Peer cleanup on disconnect — authenticated peers and coords removed when QUIC connection drops",
        "Gossip protocol wiring — GossipProtocol spawned in sync loop (15s, fanout=3), STOQ CONN_TYPE_GOSSIP=0x03, available_assets from ShardStore",
        "Stream type discrimination — 0x00=handshake, 0x01=peer_msg, 0x02=metrics, 0x03=gossip with message_handlers dispatch",
        "Swarm demand tracking — SwarmDemandTracker records per-shard fetch counts, timestamps, unique requesters from TAG_SHARD_FETCH messages",
        "NGauge intelligence bridge — feed_swarm_analytics(), compute_propagation_weights(), apply_path_recommendation() (feature-gated `intelligence`)",
        "BlockPropagator routing weights — PropagationWeight integration, peers sorted by ngauge-computed weight, zero-weight filtered",
        "Bilateral PoS verification on peer auth",
        "Reflector health scoring and pruning",
        "Cross-node metrics pipeline (MetricsReporter)",
        "NGaugeBridge periodic feed + transmit",
        "ReplicationTrigger wired to IntelligenceLayer",
        "RoutingAdvisor weights in TransactionRouter",
        "Privacy tier mapping fix",
        "BlockHeader — lightweight header for selective chain reconstruction without full block data",
        "Shard refcount dedup — content-addressed dedup with DedupPolicy (Full/HashOnly/None), auto-GC at refcount 0 (R4)",
        "Privacy-scoped deduplication (R4 — DedupPolicy::HashOnly for Anonymous: detects dupes, saves storage, no refcount/provider tracking)",
        "Genesis adoption protocol — nodes join networks by adopting foreign genesis, then sync headers",
        "SyncMessage extensions — GenesisRequest/Response, HeaderRequest/Response, BlockRequest/Response",
        "ShardLocationIndex — shard-to-provider mapping for swarm discovery (R12)",
        "TAG_SHARD_ANNOUNCE wire protocol — nodes announce shard availability to peers",
        "Consumer-becomes-provider — post-fetch store + announce for swarm scaling (R12)",
        "Selective chain reconstruction — header-only storage for non-participating segments, prune_to_headers()",
        "Post-handshake network_id metadata exchange — peers exchange network_id after STOQ handshake, backward-compatible with old nodes",
        "R7/R8 cipher compliance — quantum-resistant encryption (Kyber-1024 KEM) is default for ALL privacy modes including Anonymous",
        "Block propagation torrent model — ContentInterested strategy: Device=none, Private=full replication, Public=reflectors+spatial+consumers, Anonymous=consumers only",
        "Send-side spatial filtering — block_relevant_to_peer() checks shard placement relevance before propagating to Public mode peers",
        "Intelligence feature enabled by default — ngauge bridge compiles and runs in production",
        "Metrics ingestion wired — handle_metrics_connection feeds frames into ngauge MetricsIngestionPipeline (no longer discarded)",
        "NGaugeBridge periodic feed — SwarmDemandTracker → SwarmAnalytics every 10s via tokio task",
        "Propagation weight feed — RoutingIntelligence → TensorWeightModifier → PropagationWeight every 15s",
        "Replication trigger — ReplicationTrigger.check() against live SwarmAnalytics, signals logged with urgency > 0.5",
        "intelligence.stats IPC endpoint — queryable metrics (shard_count, peer_count, uptime)",
        "10-50 node simulation harness — 9 integration tests (asset lifecycle, ngauge feedback, shard loss, byzantine rejection, scaling, dedup, e2e loop)",
        "ShareInvite protocol — signed envelope with shard map + Kyber-wrapped DecryptionKey, FALCON-1024 signature, TAG_SHARE_INVITE wire protocol",
        "Key wrapping — encrypt/decrypt DecryptionKey with recipient's Kyber-1024 public key (KEM + BLAKE3-derived AES-256-GCM)",
        "InboxStore — async inbox with JSON persistence for received share invitations",
        "Share IPC handlers — share.send, share.inbox, share.accept, share.reject + identity.pubkey, peer.pubkey",
        "Share CLI — hypermesh share send/inbox/accept/reject/pubkey/peer-pubkey subcommands",
        "Sharing integration tests — 10 scenarios (full lifecycle, wrong key, invalid signature, multi-node fetch, persistence, KyberSegmented)",
        "Unified asset.register IPC — single handler for all asset types (system + application), replaces ad-hoc mutation paths",
        "Catalog type publishing — register_type() with BLAKE3 content-addressed schemas, type.publish + type.lookup STOQ API",
        "Built-in type definitions — Message, Invitation, Document pre-registered in Catalog with JSON Schema validation",
        "DirectMessage — Kyber-1024 encrypted body, FALCON-1024 signed, threading via reply_to, Application('Message') asset category",
        "MessageStore — blockchain-backed + YAML persistence, list_for_recipient, history_with_peer, Application asset registration",
        "TAG_DIRECT_MESSAGE (0x06) wire protocol + handler for peer-to-peer message delivery",
        "Message IPC handlers — message.send, message.inbox, message.history, message.read",
        "Message CLI — hypermesh message send/inbox/history/read subcommands",
        "Messaging integration tests — 8 scenarios (full lifecycle, threading, wrong key, invalid signature, history ordering, persistence)",
        "Identity distribution — KeyRotationEntry written to blockchain, TAG_KEY_ROTATION (0x08) wire protocol, validate_key_continuity(), split-brain detection",
        "DNS popularity tracking — DnsPopularityTracker records resolution frequency, feeds ngauge SwarmAnalytics",
        "Network DNS resolution — TAG_DNS_RESOLVE/RESPONSE wire protocol, resolve_from_network() queries peers when local fails",
        "Service IPC handlers — caesar.overview/balance/transactions/rewards/staking, ngauge.capacity/traffic/throttle/routing, trustchain.status/certs/identity/federation, stoq.stats/connections/performance (16 methods, all local crate data via daemon)",
        "EbpfFeedbackAdapter wires ngauge routing intelligence into kernel-level BPF maps",
        "Consumer-becomes-provider (R12) closed loop — post-fetch announce broadcast over STOQ, ShardLocationIndex shared between PeerContext and IPC daemon, replication poll (30s) drives extra-replica fetches via TAG_SHARD_FETCH",
        "NGaugeBridge::check_replication_signals — wraps ReplicationTrigger.check() so blockmatrix swarm coordinator can act on ngauge popularity signals",
        "NGaugeTrustAdapter — implements trustchain TrustSignalProvider trait, exposes per-peer ngauge signals (activity/capacity/traffic) to FederationManager.add_peer trust gating",
        "DaemonState federation_manager + threshold_coordinator fields — Optional Arcs gate the trustchain.request_cert IPC handler to use threshold signing when federation is opted in",
        "Cross-network transfer wire protocol — TAG_TRANSFER_LOCK/REGISTER_REQ/REGISTER_ACK/RELEASE/ROLLBACK over STOQ, alpha-default inert via Option<Arc<TransferCoordinator>> on DaemonState",
        "TransferCoordinator orchestrator — Initiated→Locked→ShardsHandedOff→Registered→Released happy path with TargetRejected / RegisterTimeout rollback paths and FederationGate short-circuit",
        "TransferReceipt cross-chain block entry — written to BOTH chains linking source_block_hash and target_block_hash so an auditor can trace transfer atomicity from either side",
        "Cross-network transfer chain-scan recovery — TransferCoordinator::resume_in_flight scans NodeBlockchain for TransferLockEntry without matching TransferReleaseEntry, reconstructs CoordinatedTransfer state, repopulates in-memory map (chain is the durable state machine; no separate snapshot file needed)",
        "Transfer wire dispatch via PeerContext.transfer_coordinator — handle_transfer_register_req routes through TransferCoordinator::handle_register_request and writes ack back over the same stream; handle_transfer_register_ack delivers oneshot acks to awaiting initiate() futures via deliver_register_ack/register_ack_waiter; alpha-default inert when transfer_coordinator is None",
        "Two-host transfer e2e integration test — bridged-transport harness exercises full source↔target choreography with JSON wire-format round-trips on every TAG; crash-recovery test drops mid-flight coordinator after Lock and asserts resume_in_flight on a fresh coordinator rediscovers the in-flight transfer",
        "Reserved-domain enforcement — top 1000 + Fortune 1000 brands + hypermesh-internal services hard-reserved; foundation CA-signed grant required for registration",
        "Foundation DNS grant flow — dns.foundation_grant IPC, FoundationGrant struct with FALCON-1024 sig, recorded as foundation.dns_grant/v1 catalog asset",
        "Network-wide DNS resolution with conflict resolution — TAG_DNS_QUERY/RESPONSE over STOQ; canonical entry by (foundation_grant_present, registration_timestamp, chain_height)",
        "DnsBlockEntry.grant_signature field — foundation FALCON-1024 signature persisted on chain, build_dns_response_for_query reads grant_signature.is_some() to set foundation_grant_present in distributed DNS responses (backward-compatible Option<Vec<u8>> via #[serde(default)])",
        "Cross-chain receipt linking — CrossChainReceiptValidator indexes TransferReceipt entries by both source and target anchors, validate_cross_chain proves source→target block linkage from either side without consulting the other chain",
        "CrossChainValidator::validate_cross_chain — looks up receipt by (source_chain_id, source_block_hash), verifies receipt's target_chain_id and target_block_hash match the request, returns specific error variants on mismatch",
        "chain.lookup_cross_receipt IPC — queries cross-chain receipt index by transfer_id, returns both source and target block hashes so an auditor can prove transfer atomicity from either side",
        "Header-only sync mode — SyncManager prefer_headers_mode flag activates HeaderRequest/HeaderResponse for low-bandwidth peers (substrate for Phase K light client); generate_sync_request emits HeaderRequest instead of full Request, headers_only_sync_count metric tracks adoption",
        "20-node real-subprocess multi-host harness — scripts/run-matrix-harness.sh orchestrator spawns N hypermesh subprocesses, blockmatrix/tests/i1_multihost_harness.rs covers 3 in-process scenarios (DNS grant attestation, concurrent receipt convergence, validator rebuild from chain) plus subprocess-driver smoke check (ignored unless HM_RUN_SUBPROCESS_HARNESS=1)",
        "StoqTransferTransport — production daemon TransferTransport wrapping NetworkManager + Weak<TransferCoordinator>; broadcast_lock/release/rollback fanout over STOQ unidirectional streams, send_register_request opens a stream to a specific peer and awaits oneshot ack delivered by wire handler via PeerContext.transfer_coordinator.deliver_register_ack",
        "Release feed subscription — foundation-signed release.feed/v1 catalog asset, FALCON-1024 verification, latest-by-channel cache (alpha-default inert until foundation pubkey configured)",
        "system.check_update + system.apply_update IPC — daemon surfaces available version with release notes URL, opt-in apply (P7: real atomic binary swap — stage to temp, fsync, chmod +x, std::fs::rename over target, schedule_restart only on success)",
        "IPC protocol_version field — major mismatch returns helpful error (-32100 PROTOCOL_VERSION_MISMATCH); minor forward-compat; old clients without field still accepted",
        "Block format V2 magic reservation (BLOCK_MAGIC_V2 = HMB\\\\x02) + format_migrations registry — future format changes have a migration path; migrate_v1_to_v2 currently identity since V2 schema == V1",
        "hypermesh update CLI command + status banner — `hypermesh status` surfaces \\\"Update available\\\" line; `hypermesh update [--channel] [--version]` invokes system.apply_update through IPC",
        "Light node mode (--mode light) — HeaderSyncManager + WitnessedProofVerifier, header-only sync, no shard/pipeline state (alpha ships flag + types + tests; full startup minimization staged as K.1.5)",
        "Capability tokens — FALCON-signed CapabilityToken with ViewOnly/Wallet/AssetWrite/Admin scopes, bound to device pubkey, daemon verification, RevocationRegistry, validate() composite check",
        "auth.create_session/list_sessions/revoke_session IPC — admin-issued tokens, alpha-default inert when capability_token_issuer is None",
        "SessionAudit chain entry — every session creation/revocation/capability-use recorded as Identity-category BlockAssetEntry with versioned JSON payload for user-side audit trail",
        "Capability-token IPC middleware — enforces required scope per method via fail-closed registry; CAPABILITY_DENIED (-32004) on missing/expired/revoked/insufficient-scope; alpha-default skips when capability_token_issuer is None",
        "Method-to-capability registry — explicit ViewOnly/Wallet/AssetWrite/Admin mapping for 50+ IPC methods, fail-closed Admin default for unknown",
        "RpcRequest.capability_token optional field — backward-compat with pre-K.2 clients (None bypasses enforcement when issuer not configured); RpcRequest::new_with_token() helper for SDKs",
        "CapabilityContext — base64 token decode + FALCON verify + expiry + revocation + scope check, returns CAPABILITY_DENIED RpcError on any failure",
        "F6 per-asset shard-fetch authorization — authorize_shard_fetch() binds TAG_SHARD_FETCH to (a) the requester's PoS proof (verify_shard_proof_binding) and (b) the shard belonging to an asset registered on our chain (authorizes_shard); a peer whose proof does not authorize that asset is refused",
        "Replication convergence actuator — post-fetch set_replica_count(shard, provider_count) so ReplicationTrigger sees needed<=replicas next cycle and STOPS (swarm-emergent convergence), closing the R12 replication loop",
        "DispersionAdvisor anti-affinity placement — extra-replica fetch consults ngauge::DispersionAdvisor for WHERE the swarm wants replicas instead of always taking candidates[0]",
        "TTL shard-location cache — provider records age like dns::cache (DEFAULT_PROVIDER_TTL 300s, is_expired sweep); expired providers are not handed out as fetch sources",
        "STOQ protocol-version negotiation — handshake carries protocol_version, major mismatch rejected BEFORE trust is granted, old peers without the field still accepted (serde default)",
        "Orphan-buffer bound — MAX_ORPHANS + 120s TTL sweep on the buffer-then-link path prevents distinct-prev-hash orphan floods from exhausting memory",
        "Matrix coordinate derivation delegates to base substrate — coordinate.rs calls base derive_cell/derive_address (fd48:4d00::/32), one canonical BLAKE3 construction shared across the workspace",
        "A1 sharding-engine move — Reed-Solomon engine (Sharder/ShardingConfig) now lives in ngauge/NGauge; blockmatrix pipeline calls it via a re-export shim (assets/pipeline/sharding.rs), byte-identical round-trip seam-tested",
        "Two-layer mirror retrieval — client-assembly fetch resolves live mirrors FIRST then the plan's canonical matrix placements (location_resolver), with a TAG_SHARD_LOCATE upstream-tracker fallback when connected peers miss",
        "Unified seed-on-fetch — client-assembly and IPC fetch paths share ONE become-provider seeder; a fetched shard is re-announced only AFTER the BLAKE3 content-validity gate passes (mirror invariant #1)",
        "share.send wire delivery — NetworkManager::send_tagged_to_peer frames [TAG_SHARE_INVITE]++invite_wire to the recipient over STOQ; receiver inbox + share.accept round-trip complete; delivered flag + graceful created_undelivered on failure",
        "Peer-auth→kernel mirror hook — mirror_peer_auth_to_kernel() calls set_peer_pos_validated(peer_ipv6, ALG_FALCON_1024) on every completed bilateral handshake, keying the kernel eBPF PoS-peer allowlist on the SAME peers userspace trusts (P5 unification / A4)",
        "Live re-announce gate — seed_fetched_shard → reannounce_authorized → blockchain.authorizes_shard: a fetched shard is only re-announced when it is content-bound on THIS node's chain; the ungated ClientAssembler seeder is doc-guarded as unwired-to-production",
        "Per-shard MIRROR INVARIANT complete on every live path — content-valid (BLAKE3) on every RECEIVE + signed-to-content (authorizes_shard) on every SERVE / RE-ANNOUNCE / BLOCK-ACCEPT",
        "On-chain shard registration at publish — IPC store handler writes a content-bound StoragePointer::Sharded BlockAssetEntry via add_block, so authorizes_shard(content_hash) returns true; store result surfaces registered_block + shard_hashes (unblocks the serve + re-seed gates)",
        "Interest-scoped registration on fetch — a served SHARD_FETCH response envelope (shard_len(4)+shard_data+registration_json, content-hash-disambiguated) carries that ONE asset's BlockAssetEntry (NodeBlockchain::registration_for_shard); the fetcher re-validates (content_binding_ok + shard-coverage + state_proof.validate) and re-anchors a FRESH block on ITS OWN chain, becoming a mirror WITHOUT syncing the server's chain (revalidate_and_register)",
        "3-node subprocess mirror E2E (gated) — tests/a6_mirror_e2e.rs (#[ignore], HM_RUN_SUBPROCESS_HARNESS=1): A publishes → B fetches + becomes mirror (announce_targets>0) → C fetches from mirror B"
      ],
      "inDevelopment": [
        "Cross-network asset transfers — production StoqTransferTransport landed in Phase I.1; full daemon opt-in (DaemonState plumbing + IPC config to enable cross-network mode) scoped to Phase J",
        "S3.1 AssetChainIndex (branch s3-asset-chain, unmerged) — per-asset lookup as a real O(1) structure replacing full-chain linear scans, many-to-many, rebuilt on load; zero block/serde format change",
        "S3.2 authenticated per-asset lineage (branch s3-asset-chain, unmerged) — prev_asset_entry + asset_seq carried INSIDE the StateProof body so lineage inherits FALCON signing and hash-commitment via proof_hash with no Block field change; tamper-evident (a forgery passing every signature check is still rejected); per-asset high-water tombstones stop a prune from re-opening an asset to a fresh root. FORMAT CHANGE: proof_hash changes for every proof, so old peers fail with a proof_hash mismatch and bincode chains will not deserialize — needs a coordinated upgrade plus `rm -rf /var/lib/hypermesh/blockmatrix/node_*` at cutover",
        "S3.3 matrix-indexed mirror attestations (in QA, unmerged) — third-party mirror attestations verified via the verify_grant template (signer_binds_to_author forbids third-party signers, so signed_proof cannot carry them), accumulated off-spine in a BTreeMap<(MatrixIndex, mirror), _> so matrix position IS the canonical order (no trusted clock), sealed into the spine by the owner via StateProof.mirror_seal; rides the same S3.2 format change",
        "Block persistence integrity — tamper detection works (BLAKE3 canonical hash verified on every read, WAL replay, legacy compat), formal security audit pending",
        "Scope-aware Public HashMatrix filtering — SpatialBucketAssigner structural code exists, not e2e tested across nodes",
        "Asset-as-file-format — SystemAssets inline in block entries, user assets as self-contained header+body units",
        "Container runtime — ProcessIsolation with real spawn, cluster management, health+heartbeat (not integration-tested)"
      ],
      "planned": [
        "MFA identity Tier 3 — recovery execution (passphrase-based key recovery, threshold peer attestation k-of-n, Shamir key backup distribution) (§6.2.3)",
        "Browser namespace — Gateway bridges HTTP/3 to HyperMesh DNS namespace (http://persist → dashboard)"
      ]
    },
    "completion": 95
  },
  {
    "id": "caesar",
    "name": "Caesar",
    "description": "Gold-denominated Ephemeral Value Protocol for HyperMesh",
    "phase": "alpha",
    "features": {
      "working": [
        "EVP core types (PacketId, GoldGrams, MarketTier, PacketState with 11 states, DemurrageRate)",
        "CaesPacket state machine (Minted->InTransit->Delivered->Settling->Settled, plus Hold/Stall/Expire/Refund/Dissolve/Disperse)",
        "Tier-based demurrage decay (L0 ~5%/hr, L1 ~0.1%/day, L2 ~0.01%/day, L3 ~0.001%/day)",
        "Governor PID controller (velocity, gold deviation, volume, liquidity, in-transit float)",
        "Constitutional fee caps (L0=5%, L1=2%, L2=0.5%, L3=0.1%)",
        "Network pressure classification (6 quadrants)",
        "Universal Payment Interface — IngressAdapter + EgressAdapter traits with 4 finality types",
        "Settlement protocol — validate, settle, split_rewards (80/20 egress/transit)",
        "Acceptance criteria — per-recipient preferences structure",
        "Gravity dissolution — 90-day timeout, 6-criteria qualification, weighted distribution",
        "Fee distribution — 80/20 egress/transit split, bytes-weighted transit allocation",
        "Gold oracle — price tracking, 10% band validation, grams/USD conversion",
        "Effective price composite (network fees + speculation pressure - liquidity shadow)",
        "Capacity-based packet routing (bandwidth/buffer/latency/load scoring)",
        "Holding buffer — orbit buffer for fee-budget-exceeded/congestion packets",
        "Packet-centric storage (PacketRecord, SettlementRecord, JSON persistence)",
        "CaesarProtocol coordinator (storage + processor + distributor + oracle + router + governor)",
        "Conservation law verification + circuit breaker",
        "MeshCredit UPI adapter (internal ledger, 1:1 CAES denomination)",
        "OracleFeed trait for pluggable gold price sources (ManualFeed for alpha)",
        "Node status tracking in storage (settled count, fee earnings)",
        "STOQ API handlers wired to real CaesarProtocol (5 handlers over QUIC)",
        "Caesar binary CLI (--bind, --service-name, STOQ API server launcher)",
        "Cross-chain bridge trait (InternalBridge with conservation checks)",
        "UI-facing API handlers — /wallet, /balance, /transactions, /rewards, /staking, /exchange/rates, /analytics/overview, /analytics/earnings (8 handlers matching UI TypeScript interfaces)"
      ],
      "inDevelopment": [
        "Multi-node packet routing — routing logic exists but packets don't actually traverse QUIC connections between nodes",
        "Blockchain-backed acceptance criteria — currently in-memory, should be on Network chain",
        "NGauge capacity metrics integration — feature-gated code exists but no real data pipeline",
        "Banking interop bridge — velocity economics functional, 3 tests failing on external adapter paths"
      ],
      "planned": [
        "Live multi-chain UPI bridge BTC/ETH/SOL (blocked by: chain SDK deps)",
        "Stripe provider (integration point is caesar-sdk UPI adapter traits; live banking providers require compliance + API credentials)",
        "Plaid provider (integration point is caesar-sdk UPI adapter traits; live banking providers require compliance + API credentials)",
        "Square provider (integration point is caesar-sdk UPI adapter traits; live banking providers require compliance + API credentials)",
        "OpenBanking provider (integration point is caesar-sdk UPI adapter traits; live banking providers require PSD2/PCI-DSS compliance + API credentials)",
        "External fiat/crypto payment rail integrations (blocked by: external API keys + compliance)",
        "Real gold oracle data feed (blocked by: external API subscription)"
      ]
    },
    "completion": 69
  },
  {
    "id": "caesar-sdk",
    "name": "Caesar SDK",
    "description": "Universal Payment Interface SDK — adapter traits for external payment rail integration",
    "phase": "alpha",
    "features": {
      "working": [
        "IngressAdapter trait (7 methods: adapter_id, supported_denominations, lock_external_value, verify_lock, release_lock, liquidity_pressure, to_gold_grams)",
        "EgressAdapter trait (5 methods: adapter_id, supported_denominations, available_capacity, settle, capacity_ratio)",
        "UPI shared types (IngressLockProof, SettlementReceipt, SettlementFinality, LiquidityPressure, UpiError)",
        "MeshCreditAdapter reference implementation (internal BlockMatrix ledger, 1:1 CAES denomination)",
        "MockIngressAdapter + MockEgressAdapter (public test utilities for adapter developers)"
      ],
      "inDevelopment": [],
      "planned": [
        "Adapter development guide and examples",
        "External payment rail adapter implementations (requires real banking/crypto integrations)"
      ]
    },
    "completion": 71
  },
  {
    "id": "catalog",
    "name": "Catalog",
    "description": "Asset package registry and template library",
    "phase": "alpha",
    "features": {
      "working": [
        "Asset package types and metadata",
        "Asset registry with typedef registration and search",
        "Template generation framework",
        "Semantic versioning",
        "Scripting engine (syntax validation only — no execution)",
        "Asset SDK (clean public API facade)",
        "Typedef registry marketplace (browsing, multi-factor search scoring)",
        "Binary publisher authentication (interface only — PoS validation is fake)",
        "BLAKE3 content hashing",
        "DHT distribution — local data structures (XOR-distance routing, k-bucket management, value store)",
        "Dependency resolution (topological sort, transitive resolution, circular detection)",
        "Dashboard manifest types (DashboardManifest parse/validate, shared with blockmatrix)",
        "Asset validation pipeline (size limits, BLAKE3 hash verification, metadata completeness)",
        "catalog.hypermesh.online STOQ API endpoint (6 handlers over QUIC)",
        "Catalog server binary stability (standalone STOQ API — LRU cache OOM bug fixed)",
        "DHT store/query over STOQ transport",
        "STOQ API handlers wired to CatalogRegistry",
        "Real FALCON-1024 signature verification",
        "Canonical PoS types consumed from lib (S1) — catalog's duplicate PoS type set was deleted; it now uses hypermesh_lib's proof types",
        "X.509 certificate parsing for publisher info",
        "TrustChain certificate validation (real expiry/revocation)",
        "Policy rule evaluation (CertificateIssuer)",
        "foundation.dns_grant/v1 typedef hosted",
        "release.feed/v1 typedef hosted (Phase J.1 — foundation-signed release announcements: version, channel, per-target binary hashes, release notes URL, FALCON-1024 signature)",
        "Registry dependency resolution — CatalogRegistry::resolve_dependencies runs a cycle-safe BFS producing DependencyGraph { direct, transitive, missing } with honest missing-tracking (unregistered hashes surfaced, no fabricated graph)",
        "Embedded typedef-reference scanner — schema_scanner::scan_schema_for_typedef_refs regex-scans a typedef schema body for 64-hex BLAKE3 refs that resolve to registered typedefs, unioning implicit deps with declared ones",
        "CatalogProvider trait + blockmatrix adapter — CatalogRegistryAdapter/wire_catalog_registry expose the registry to blockmatrix via a dyn-safe provider (resolves the catalog→blockmatrix dependency cycle); catalog.dependencies IPC handler is backed by the real BFS when a registry is present, honest alpha-inert when absent"
      ],
      "inDevelopment": [
        "Caesar contribution reward integration — CatalogRewardAdapter exists but no real Caesar EVP connection",
        "Peer-to-peer sharing — announce/discover code exists but local-only until DHT remote I/O is wired",
        "HyperMesh execution delegation — allocate/query/terminate lifecycle code exists but no real remote execution",
        "Shared asset library — browse and install packages across nodes on the public network"
      ],
      "planned": []
    },
    "completion": 87
  },
  {
    "id": "desktop",
    "name": "Desktop Shell",
    "description": "Tauri 2 native desktop shell — system tray, daemon subprocess manager, first-run wizard, embedded WebView for the React UI",
    "phase": "alpha",
    "features": {
      "working": [
        "Tauri 2 project scaffolding (separate from workspace, own Cargo.toml)",
        "tauri.conf.json: bundle config, identifier, dev/prod URLs, all platform targets",
        "System tray: HyperMesh header, status label, Open Dashboard, Start/Stop daemon, Update available, Quit",
        "Tray icon state colours (green/yellow/red) via state-icon swap",
        "Daemon subprocess manager (spawn/stop/status, IPC ping over Unix socket)",
        "Tauri commands surface: daemon_start/stop/status, daemon_check_update, wizard_should_show/state/set_*/complete",
        "First-run wizard scaffolding (6 React pages: welcome → privacy → identity → trustnet → grant → done)",
        "Tauri bridge with graceful no-op fallback (UI builds standalone for Gateway use case)",
        "UpdateBanner ↔ tray bridge (update-available event from daemon's system.check_update IPC)",
        "Hide-to-tray on window close (true exit only via tray Quit)",
        "Daemon stop on shell exit (best-effort SIGTERM → SIGKILL fallback)",
        ".gitignore for target/, gen/, binaries/, bundle artifacts",
        "Placeholder PNG/ICO/ICNS icons for compile-time embedding",
        "release.yml `desktop` matrix job (Linux/macOS x86+aarch64/Windows, continue-on-error)",
        "Daemon binary uploaded as workflow artifact for sidecar staging",
        "docs/RELEASING.md desktop section with sysdeps + local-build instructions"
      ],
      "inDevelopment": [
        "Tauri sidecar daemon binary integration — release.yml stages it but tauri.conf.json externalBin config not yet wired (C.3.5)",
        "Real platform icons — placeholders shipped, to replace via `cargo tauri icon source-1024.png` (C.3.5)",
        "Identity fingerprint display in wizard Page 3 — currently shows path; needs dedicated `core.identity` IPC (C.3.5)",
        "Foundation grant submission via dns.foundation_grant IPC — wizard records intent only (post-J)"
      ],
      "planned": [
        "Windows named-pipe IPC for daemon ping (currently Unix-socket only)",
        "Auto-update flow inside the desktop bundle (current path: re-install via package manager)",
        "macOS code-signing + notarization in release.yml (manual today; see docs/RELEASING.md C.2 section)",
        "Tauri permissions hardening — capabilities/default.json grants broad webview access",
        "Multi-window support (separate Caesar wallet window, NGauge dashboard window)"
      ]
    },
    "completion": 64
  },
  {
    "id": "gateway",
    "name": "Gateway",
    "description": "HTTP/3 + STOQ gateway for *.hypermesh.online — 4 roles: bootstrap, inbound proxy, outbound proxy, inter-network",
    "phase": "alpha",
    "features": {
      "working": [
        "QUIC/HTTP3 server setup (quinn + h3)",
        "TLS provider (File/TrustChain/SelfSigned certificate sources)",
        "Connection pool with health checks",
        "Router with path-based backend selection",
        "Circuit breaker and retry logic",
        "CORS middleware and request logging",
        "HTTP/3 backend proxying (gateway forwards HTTP/3 to TCP backends, NOT STOQ)",
        "Rate limiting and DDoS protection (token bucket per-IP/identity/global)",
        "Load balancing (RoundRobin/LeastConnections/WeightedRoundRobin/HealthAware)",
        "Multi-domain SNI routing (*.hypermesh.online wildcard support)",
        "Inbound proxy for HyperMesh dashboards",
        "Health check aggregation — /health checks all 5 backend services",
        "TrustChain backend routing (gateway -> [::1]:8444)",
        "BlockMatrix backend routing (gateway -> [::1]:9292)",
        "Caesar backend routing (gateway -> [::1]:9294)",
        "Catalog backend routing (gateway -> [::1]:9295)",
        "NGauge backend routing (gateway -> [::1]:9296)",
        "NGauge API routing (/api/v1/ngauge)",
        "Scope-aware dashboard server (DashboardServer with scope determination, content-type detection, caching)",
        "Dashboard load_from_directory and load_defaults methods for populating scope-aware cache",
        "trust.hypermesh.online onboarding dashboard (public landing, private topology, admin controls)",
        "STOQ protocol bridge — stoq_bridge.rs and stoq_listener.rs wired into main.rs, runs alongside HTTP/3 on port 8444 (configurable), graceful degradation if port unavailable",
        "Production config loader — GatewayConfig::load() cascades GATEWAY_CONFIG env → ~/.hypermesh/gateway.toml → defaults, with per-field env overrides",
        "DNS-over-HTTPS (DoH) endpoint — JSON-based DNS query/response for browser-compatible resolution",
        "Host→DNS resolution — extract_subdomain() maps Host header to blockchain DNS entries",
        "Dynamic per-domain TLS certificates — generate_domain_cert() creates ECDSA P-256 self-signed certs on demand",
        "DomainRouter.resolve_from_dns() — query blockchain DNS for domain→backend routing",
        "RemoteDashboardProxy — byte-forwarder over an existing STOQ stream (X25519MLKEM768 transport encryption + FALCON-PoS auth provided by STOQ; Kyber-AES is the asset-at-rest layer not used here); capability token rides in the forwarded envelope for target-side auth",
        "Three remote-dashboard modes — DashboardServeMode::TrustProxy / PrivateDomain / SelfHosted (parses --dashboard-mode CLI flag)",
        "RemoteProxyManager — per-session registry with register/get/close/reap_inactive lifecycle",
        "ForwardedRequest/ForwardedResponse — minimal HTTP envelope shape forwarded over the STOQ stream",
        "StoqStreamForwarder trait — abstracts STOQ stream open/write/read for the proxy; MockStreamForwarder ships for tests, production wiring binds Connection::open_stream from gateway's NetworkManager handle",
        "StoqAuthMode { HttpProxy (default) | FullStoqPos } — config + CLI + env parse; HttpProxy is a plain reverse proxy (no PoS, backwards-compatible), FullStoqPos requires a bilateral PoS handshake per connection",
        "FullStoqPos handshake enforcement — StoqListener runs StoqBridge::perform_pos_handshake on accept and DROPS un-authenticated connections; StoqBridge::new_full_stoq_pos requires a NodeSigner + StateProofProvider (inherits the F2 signer↔identity binding)",
        "NodeSigner + StateProofProvider wired into StoqBridge — gateway depends on trustchain for FALCON-1024 identity + TrustChainProofProvider (path dep, no cycle: trustchain does not depend on gateway)",
        "Dual-CA integration — gateway connects to the clearnet CA at trust_chain_addr=[::1]:8444 (trustchain-http3.service); systemd unit requires trustchain-http3.service (the single trustchain.service was retired in the F9 split)",
        "Gateway's own STOQ listener disabled by default — stoq_listen_addr omitted (Option→None) so port 8444 is left to trustchain-http3; gateway serves clearnet over HTTP/3 on 8443, with stoq_max_connections bounding the STOQ bridge"
      ],
      "inDevelopment": [
        "Production StoqStreamForwarder — wire MockStreamForwarder replacement that opens a real STOQ stream via Connection::open_stream and round-trips request bytes; depends on gateway-side NetworkManager handle reaching remote_proxy",
        "HTTP/3 bootstrap-token validation — token issued via HTTP/3 is still NOT cross-validated at the STOQ layer; FullStoqPos now enforces a real bilateral PoS handshake per connection, but the HTTP/3-issued bootstrap token is not yet bound to that handshake",
        "Cross-scope routing — ScopeRouter exists but Device/Network scope bridging not functional (Network scope not implemented)",
        "Federation bridge — trust level structs exist but no real cross-network STOQ relay (no federated peers)",
        "Cross-scope transfer proxy — Phase G.1 wire protocol + coordinator + G.2 chain-scan recovery + PeerContext wire dispatch landed in blockmatrix (5/5 g1_transfer + 4/4 g2_transfer tests pass); gateway-side cross-network proxying still pending (e2e routing of TAG_TRANSFER_* through GatewayManager scoped to Phase I)",
        "Outbound proxy with allowlist filtering — code exists, not tested",
        "Backend service discovery (resolve by address or service registry)"
      ],
      "planned": [
        "PoS authentication subsystem — Phase 2, beyond Phase 0 bootstrap (self-signed + PoS handshake at STOQ layer is sufficient). Former AuthManager/BootstrapHandler/authenticate_stoq_with_proof was never wired into the request pipeline and has been removed. Can be resurrected from git history when multi-tenant HTTP/3 auth is needed.",
        "Bootstrap token flow — HTTP/3 side issues tokens but validation path beyond self-serve PoS handshake not implemented"
      ]
    },
    "completion": 80
  },
  {
    "id": "hypermesh",
    "name": "HyperMesh Repository",
    "description": "Cross-cutting repository-wide milestones and meta-tasks",
    "phase": "alpha",
    "features": {
      "working": [
        "Refactor / consolidation — removed Docker/Terraform/K8s/CloudFormation (system-level daemon model), fixed CI workflows, workspace lint config",
        "Documentation 100% correct — all 11 crate READMEs, ARCHITECTURE.md, root README accurate with correct stats, models, and IPv6 coverage",
        "Benchmarks — Criterion benchmarks for blockmatrix (5: phase1, retrieval, transfer, tensor, basic), stoq (3: throughput, real_throughput, ebpf), trustchain (certificates)",
        "Full development/contributions pipeline — CONTRIBUTING.md, SECURITY.md, CHANGELOG.md, pre-commit hooks, PR workflow documented",
        "Bug tracking/reporting — GitHub issue templates (bug report, feature request), severity labels, security disclosure via SECURITY.md",
        "Cross-crate integration tests — 33 compile-time tests in cross_crate_integration.rs + 17 integration tests (asset_pipeline, blockchain_scope, privacy_consistency)",
        "Unified cross-crate identity model (NodeId BLAKE3([u8;32]), ScopedIdentity, WorkloadType — all crates use lib canonical types)",
        "Scope-aware identity system (IdentityScope respects Device|Network scope + traceability axis, cert extensions, strategy scoping)",
        "Certificate lifecycle per scope (Anonymous=ephemeral minutes, Private=bounded days, Public=full months — trustchain + stoq)",
        "5 language SDKs (TypeScript, Python, C#, C++, Go) — HTTP REST + FFI dual-path, per-service modules, examples, guides",
        "C FFI bindings (hypermesh-ffi) — 38 functions covering all APIs, 5 language FFI loaders",
        "CLI service subcommands (hypermesh caesar/trustchain/ngauge/catalog) with IPC relay",
        "Shard architecture — disk persistence, auth TTL, peer cleanup on disconnect, PoS-gated access control; kernel+userspace policy unified and shard fetch is PoS-authenticated (eBPF kernel gate)",
        "Workspace version unification — [workspace.package] version = 1.0.0, all 13 members inherit version.workspace (P7 fixed the prior ngauge/caesar 1.0.0-vs-0.1.0 skew)",
        "One-click installer — scripts/install/hypermesh-install.sh (P7)",
        "S1 Proof of State consolidation — ONE canonical PoS type set in lib (StakeProof/WorkProof/SpaceProof/TimeProof/StateProof/WireSignedProof + Owner/GrantScope/AuthorizationSet/AuthDecision/CapacityProfile); trustchain re-exports and keeps generation/crypto via a StateProofOps extension trait; stoq + catalog consume lib's set; 7 duplicate PoS type sets removed across trustchain/stoq/blockmatrix/caesar/catalog",
        "S1 stake-magnitude → authorization — every magnitude gate deleted workspace-wide (stake_amount, minimum_stake, MAX_REASONABLE_STORAGE, PoW leading-zero difficulty + nonce including hypermesh_xdp.c, capacity admission gates); PoStake is WHO, PoWork is a work_hash, PoSpace capacity is descriptive and never gates",
        "S1 fixed the `--features multi-node` build (was E0072 recursive-type on main)",
        "PoS magnitude guard — scripts/check-no-pos-magnitude.sh scans the whole worktree by behaviour/shape (not identifier, not limited to src/) and blocks on pre-push",
        "VISION.md — architecture of record, including a Security Model section (genesis is self-signing; secrecy is singular, proof is plural)"
      ],
      "inDevelopment": [
        "Live alpha deployment — trust.hypermesh.online first public node (gateway + trustchain CA/DNS/CT + blockmatrix reflector); dual-CA split (http3:8444 / stoq:8445) deployed and verified live (F9)",
        "Network MVP — bilateral handshake + block sync verified E2E local-to-GCP (trust.hypermesh.online), network_id metadata exchange, runtime block propagation, cross-genesis sync, real FALCON-1024 PoS, PoS-gated access control, shard distribution, stale cert auto-recovery.",
        "Mirror/sharding architecture correction (A-series) — Reed-Solomon sharding moved into NGauge, two-layer mirror retrieval + upstream tracker, per-shard mirror invariant on live paths, on-chain shard registration at publish + interest-scoped registration on fetch, 3-node mirror flow proven E2E",
        "Per-asset chain (S3.x, branch s3-asset-chain, NOT merged) — S3.1 AssetChainIndex, S3.2 authenticated per-asset lineage in the StateProof body, S3.3 matrix-indexed mirror attestations (in QA). S3.2 is a FORMAT CHANGE: proof_hash changes for every proof, so old peers fail with a proof_hash mismatch and bincode chains will not deserialize — coordinated upgrade plus `rm -rf /var/lib/hypermesh/blockmatrix/node_*` required at cutover",
        "Handshake/identity layer migration — FalconIdentity, bilateral handshake, and PoS exchange must move from blockmatrix to STOQ/TrustChain (transport-layer concerns in blockchain crate)"
      ],
      "planned": [
        "Modular restructure for polyrepo/microservice — extract crates into independent repos with CI, versioned APIs, and publish pipeline",
        "Development documentation live sync — auto-generate and publish docs from source (rustdoc, typedoc) on every merge",
        "Website linking restructure to polyrepo and live docs — update hypermesh.online to link per-repo docs, changelogs, and dashboards",
        "Usability testing — real-user testing of CLI, dashboard, and asset workflows with feedback collection and iteration",
        "Remove all hardcoded performance stubs (tests/performance.rs, tests/validation.rs, tests/chaos.rs)",
        "Real partition/chaos testing framework"
      ]
    },
    "completion": 65
  },
  {
    "id": "hypermesh-ebpf",
    "name": "HyperMesh eBPF",
    "description": "Unified eBPF intelligence layer - single source of truth for all packet processing",
    "phase": "alpha",
    "features": {
      "working": [
        "HyperMeshEbpf orchestrator API — capability detection, state management, graceful degradation (Full eBPF+AF_XDP -> eBPF only -> userspace)",
        "AF_XDP zero-copy I/O — 4-ring UMEM buffers, frame allocator, batch send/receive via syscalls",
        "C kernel XDP programs — counter, kprobe, tracepoint, HyperMesh XDP with plaintext 0x484D ('HM') extension-header parse (papers §5.1-5.7). IPv4-SAFE BY CONSTRUCTION: every non-HyperMesh / non-port-9292 / no-0x484D / bounds-fail path returns XDP_PASS, never DROP (permanent gateway-outage fix)",
        "eBPF PoS-peer connection allowlist (A4) — kernel policy_map[src_ip].requires_pos (derived from privacy tier: Anonymous=0, Private/Public=1) gated against pos_header_map[src_ip].validated; a Private/Public source not yet PoS-validated is XDP_DROP, all else PASS; allowlist populated live by mirror_peer_auth_to_kernel on bilateral PoS handshake success",
        "0x484D PoS substrate restored + wired (A4-CORRECT, reverses the A4/F10 'phantom' deletion) — hypermesh_headers.rs WirePosHeader/AssetHashHeader/MatrixRoutingHeader structs, STOQ emits the plaintext 0x484D + WirePosHeader prefix on the send path (offset-pinned byte-identity tests vs the C hmesh_* structs), two-tier validation (kernel pre-validate format/algorithm + userspace FALCON deep-verify feeding pos_header_map/policy_map back via set_peer_pos_validated), verifier-safe per-flow conntrack",
        "AF_XDP kernel integration — xsk_map redirect, UMEM mmap lifecycle",
        "BPF map policy enforcement — 32-byte LE serialization and sync_to_kernel",
        "PacketDecision three-path routing — Pass (local), Redirect (AF_XDP->STOQ), Forward (XDP_TX), Drop",
        "Validation hooks — STOQ (certificate, packet) registered (A3: dead BlockMatrixExtensionValidator with 0 callers + 6 dead stoq header/validator re-exports were grep-verified dead and pruned)",
        "Asset hash verification (BLAKE3) and blockchain registry validation",
        "Privacy tier eBPF enforcement with per-policy flag control",
        "Orchestrator state storage — routing rules, asset hashes, PoS validation state",
        "build.rs auto-compilation of C eBPF programs (ebpf-loader feature gate)",
        "Unified intelligence + transport metrics collection — HyperMeshMetrics (5 categories) consumed by STOQ MetricsFrameBridge for Congestion/Routing frames",
        "Multi-queue AF_XDP load balancing (RoundRobin/LeastLoaded/FlowHash strategies)",
        "Hardware offload detection and opportunistic NIC offload",
        "PoS structural pre-validation — timestamp freshness, algorithm indicator, IPv6/matrix position checks (S1 removed the PoW difficulty target and nonce: PoWork is a content-hash match, not a mining contest; full crypto deferred to userspace TrustChain by design)",
        "Matrix routing path validation — source/destination parsing, loop detection, coordinate bounds checking",
        "KernelPosConfig — configurable timestamp skew, validation TTL, enabled flag; 24-byte serialization matching C struct pos_config (S1 dropped the difficulty word, 32->24, wire layout preserved; regression-guarded by test_kernel_pos_config_carries_no_difficulty_field)",
        "Kernel-attach BPF map writes for PoS validation cache — update_pos_header_map does a real aya HashMap<[u8;16],[u8;24]> insert under `kernel-attach` (validated + algorithm + last_validated via CLOCK_MONOTONIC), no-op otherwise; makes the XDP `now - last_validated > ttl` comparison meaningful",
        "NGauge → eBPF feedback impl (live) — blockmatrix EbpfFeedbackAdapter calls set_routing_rule()/set_privacy_tier() on HyperMeshEbpf; PoS handshake success drives set_peer_pos_validated (ALG_FALCON_1024) via blockmatrix mirror_peer_auth_to_kernel, writing pos_header_map.validated=1 + policy_map.requires_pos=1",
        "libbpf-sys dead dep removed — it was declared but never referenced (loader is aya, pure Rust); it only broke the musl static-pie deploy build (vendored-C UAPI conflict). Removal unblocks `cargo build --target x86_64-unknown-linux-musl`; the aya XDP kernel gate is unaffected",
        "Cross-platform compile — Linux-only deps (aya/aya-log/libc) target-gated via [target.'cfg(target_os = \\\"linux\\\")'.dependencies] in Cargo.toml; non-Linux gets userspace-only no-op stubs that satisfy HyperMeshEbpf API surface (set_routing_rule, set_privacy_tier, metrics() return Ok with no-op). All aya usage in source is #[cfg(feature = \\\"kernel-attach\\\")] gated. Verified: cargo tree shows aya absent from macOS/Windows dep trees, present only on Linux."
      ],
      "inDevelopment": [
        "Real multi-node traffic testing for matrix routing and PoS validation pipeline",
        "Datapath caveat — the 0x484D header is only kernel-visible on the AF_XDP raw path; production sends via Quinn QUIC (encrypted, kernel-invisible), so header-on-wire is deferred (not deployed to gateway until settled)"
      ],
      "planned": [
        "Production multi-node eBPF policy distribution"
      ]
    },
    "completion": 88
  },
  {
    "id": "hypermesh-ffi",
    "name": "HyperMesh FFI",
    "description": "C FFI bindings for the HyperMesh SDK — foundation for TypeScript, Go, Python, C/C++, C# integrations",
    "phase": "alpha",
    "features": {
      "working": [
        "Opaque client handle (hypermesh_connect/disconnect with tokio runtime)",
        "Raw JSON-RPC call (hypermesh_call — any IPC method accessible from C)",
        "Thread-local error state (hypermesh_last_error, set on failure)",
        "Memory management (hypermesh_free_string for caller-owned strings)",
        "Node API (hypermesh_status)",
        "DNS API (hypermesh_dns_resolve, dns_list, dns_register)",
        "Network API (hypermesh_peers)",
        "Blockchain API (hypermesh_blockchain_height, blockchain_block)",
        "Topology API (hypermesh_topology_info)",
        "Asset API (hypermesh_asset_list, asset_store, asset_fetch)",
        "Domain API (hypermesh_domain_list, domain_register)",
        "Dashboard API (hypermesh_dashboard_list, dashboard_deploy)",
        "Config API (hypermesh_config_show, config_get)",
        "C header file (include/hypermesh.h with extern C guards)",
        "NULL-safe — all functions handle NULL client gracefully",
        "Direct Identity ABI (hypermesh_identity_generate/load/free/node_id/public_key/kyber_public_key/sign)",
        "Direct signature verify (hypermesh_verify_signature — stateless FALCON-1024)",
        "Direct PoS proof verify (hypermesh_signed_proof_verify — WireSignedProof envelope, preserves PoS gate)",
        "Direct AssetAddress ABI (new/to_ipv6/from_ipv6/fingerprint/coords/shard_index/is_hypermesh)",
        "Direct content-hash mirror invariant (hypermesh_compute_content_hash/hypermesh_verify_content_hash — BLAKE3, R4)",
        "cbindgen-generated header (build.rs + cbindgen.toml regenerate include/hypermesh.h)",
        "catch_unwind at every direct-ABI boundary; opaque handles with matching free fns",
        "C smoke test (tests/smoke.c + run_smoke.sh) links staticlib and exercises the direct ABI",
        "hypermesh_publish (identity, bytes, len) — emits BLAKE3[32] content address AND a serialized WireSignedProof whose FALCON-1024 signature is over BLAKE3(proof_bytes||nonce), byte-identical to TrustChainProofProvider::generate_proof (NOT a signature over the raw asset bytes); reuses NodeSigner::sign + compute_blake3_hash, no reimplemented crypto",
        "hypermesh_verify_proof (wire_proof, len) — verify-only, mirrors TrustChainProofProvider::validate_proof; holds no key and cannot mint a valid proof",
        "hypermesh_falcon_signature_max_len() = 1280 (PQClean FALCON-1024 authoritative sig length) for caller buffer sizing",
        "SECURITY-CRITICAL FALCON seam — the FALCON secret key lives only inside the opaque hypermesh_identity_t handle and NEVER crosses the C ABI boundary; publishing requires the handle, verification is keyless",
        "Caesar API (hypermesh_caesar_wallet, balance, transactions, rewards, route_packet, governor_params)",
        "TrustChain API (hypermesh_trustchain_certificates, issue, validate, revoke, dns_zones)",
        "NGauge API (hypermesh_ngauge_capacity, traffic, marketplace, node_metrics, leases)",
        "Catalog API (hypermesh_catalog_browse, search, package_info, registry_stats)",
        "TypeScript FFI loader (ffi-napi + ref-napi, optional dependency)",
        "Python FFI loader (ctypes wrapper with library discovery)",
        "C# FFI loader (P/Invoke with cross-platform NativeLibrary resolver)",
        "Go FFI loader (cgo wrapper with build tag)",
        "C++ FFI loader (direct linking via FfiClient class + CMake target)"
      ],
      "inDevelopment": [],
      "planned": []
    },
    "completion": 100
  },
  {
    "id": "hypermesh-lib",
    "name": "HyperMesh Lib",
    "description": "Shared canonical types for the HyperMesh ecosystem",
    "phase": "alpha",
    "features": {
      "working": [
        "NodeId, AssetId, NetworkId, and ContentHash newtypes",
        "PrivacyMode struct (2-axis: AccessScope + tracked)",
        "BlockchainScope enum (Device | Network)",
        "ProofType enum (PoSpace/PoStake/PoWork/PoTime)",
        "MatrixPosition coordinate type",
        "PipelineStage enum",
        "CryptoAlgorithm enum (Falcon/Kyber/AES)",
        "HypermeshError unified error type",
        "Three-pillar asset system (AssetKind + BaseState/AssetStatusTrait + AssetAdapter)",
        "Canonical state proof types (SpaceProof/StakeProof/WorkProof/TimeProof/StateProof/WireSignedProof + Validatable trait) — S1 made lib the ONE definition site; 7 duplicate PoS type sets removed from trustchain/stoq/blockmatrix/caesar/catalog",
        "Authorization model (S1) — StakeProof is WHO/authorization with NO amount, WorkProof carries work_hash (no difficulty/nonce/compute), SpaceProof is WHERE/location (capacity is descriptive and never gates), TimeProof is WHEN",
        "Authorization types (lib/src/authz.rs) — Owner, GrantScope (Read|Use|Transfer), AuthorizationSet, AuthDecision, CapacityProfile",
        "Determinism seam — *_at() constructors take an explicit timestamp so the genesis path is a pure function of its inputs (S3.0); live paths keep SystemTime::now()",
        "EVP economic types (PacketId, GoldGrams, MarketTier, PacketState, DemurrageRate)",
        "AssetAddress IPv6 type (fd48:4d00 prefix, matrix coords, content fingerprint, shard sub-addressing)",
        "Transmission asset type in SystemAssetKind (R10)",
        "SystemAssetKind::Identity and KeyRotation variants for MFA identity lifecycle (§6.2)",
        "ErasureCodingParams with adaptive shard sizing (R14 — for_asset_size scales RS parameters)",
        "Shard, ShardMetadata, ShardingStats, ShardingError types (A1 — moved here from blockmatrix as part of the sharding→NGauge move; live in lib so consumers avoid an ngauge dependency cycle, serde layout unchanged; Shard::calculate_hash/verify are BLAKE3 integrity helpers)",
        "HardwareCapabilities with minimum spec enforcement (R1/R13 — meets_minimum_spec validation)",
        "BLAKE3 validation helpers (validate_blake3_hash, compute_blake3_hash)",
        "ShardCommitment type (R12 — BLAKE3(sorted placements) compute/verify API)",
        "GenesisAssetRecord type (R1+R10 — capability structs for genesis inventory)",
        "Shared serialization via postcard (encode/decode/encode_bounded)",
        "Unified NodeId type (BLAKE3([u8;32]) from FALCON-1024 pubkey)",
        "Scope-aware identity types (WorkloadType, IdentityScope, ScopedIdentity)",
        "Cross-crate validation helpers (validate_node_id, validate_content_hash, etc.)",
        "Runtime state types (NodeState, NetworkState, SyncStatus, RuntimeSnapshot)",
        "Common test utilities (test fixtures for all canonical types)",
        "Public SDK types (SdkCapabilities, AssetDescriptor, NodeDescriptor, QueryResult<T>)"
      ],
      "inDevelopment": [
        "State proof depth — lib defines the canonical types and structural validity; generation/crypto lives in trustchain via the StateProofOps extension trait, and per-proof validity is still structural (identity binding present, timestamp bounds), not threshold-verifying",
        "BlockchainScope::Network — blockmatrix implements Network scope sync MVP (block propagation, gossip, cross-genesis insertion, E2E two-node over QUIC)"
      ],
      "planned": []
    },
    "completion": 94
  },
  {
    "id": "hypermesh-sdk",
    "name": "HyperMesh SDK",
    "description": "Typed Rust client SDK for the HyperMesh daemon IPC API",
    "phase": "alpha",
    "features": {
      "working": [
        "HyperMeshClient with Unix socket IPC connection and raw_call()",
        "Node API (status, coordinate, privacy mode, uptime)",
        "DNS API (resolve, list entries)",
        "Asset API (list, info, store, fetch)",
        "Network API (peers, connect)",
        "Blockchain API (height, block by index, chain validation)",
        "Topology API (info, neighbors, routing cost, path finding)",
        "Domain API (register, list, join)",
        "Dashboard API (deploy, list, info)",
        "Config API (show, get, set)",
        "SdkError enum (Connection, Ipc, Rpc, Serialization, Timeout, NotConnected)",
        "Typed response structs with serde deserialization"
      ],
      "inDevelopment": [],
      "planned": [
        "Event subscription (streaming daemon events)"
      ]
    },
    "completion": 92
  },
  {
    "id": "mobile",
    "name": "Mobile Shell",
    "description": "React Native + Expo mobile shell — iOS + Android remote clients connecting to the user's trusted daemon (home server, trust gateway, or self-hosted) via Phase K.2 capability tokens. Phone is never a node.",
    "phase": "alpha",
    "features": {
      "working": [
        "Expo SDK 50 React Native project structure (App.tsx + src/)",
        "package.json with @hypermesh/sdk via file: protocol + expo + RN + RN nav + RN query + NativeWind",
        "tsconfig.json strict TS + expo base + paths alias",
        "babel.config.js + metro.config.js + tailwind.config.js + global.css for NativeWind 4",
        "app.json: name HyperMesh, bundleIdentifier online.hypermesh.app, expo-secure-store plugin, ITSAppUsesNonExemptEncryption=false",
        "eas.json: development/preview/production profiles, ios+android submit config templates",
        "DeviceFingerprint: Expo SecureStore key persistence, WebCrypto P-256 placeholder keygen, algorithm tag for C.4.5 migration",
        "ChallengeSign: WebCrypto ECDSA-P256 challenge signing, RequestedCapability type matching K.2 caps",
        "TokenStore: capability token + expiry + sessionId + baseUrl + ConnectionMode in SecureStore, loadValidSession refresh window",
        "MobileApiClient: bootstrap from persisted session, invoke wrapper catches -32004 and emits reauth events, fetchChallenge fallback to synthetic",
        "ConnectScreen: 3-mode picker (trust-gateway/private-domain/self-hosted), URL input, fingerprint preview with alpha badge, Connect button drives challenge→sign→session",
        "DashboardScreen: node.status + network.peers polling every 30s, RefreshControl, sign-out button (revoke + clear)",
        "AssetsScreen: caesar.balance + asset.list read-only with TanStack Query refetch",
        "ShareScreen: expo-document-picker file pick + share.send POST + share.inbox poll (route 404 falls back to empty)",
        "MessageScreen: message.inbox poll + message.send compose form",
        "DnsScreen: dns.resolve lookup + dns.list own records",
        "RootNavigator: Connect | bottom-tabs (Home/Assets/Share/Messages/DNS), DarkTheme palette match, capability-denial reauth bounce",
        "App.tsx: QueryClientProvider + SafeAreaProvider + StatusBar light",
        ".gitignore: node_modules/, ios/Pods, android/build, .expo, *.ipa/*.apk/*.aab, assets/ placeholder",
        "README.md: full developer setup, EAS build runbook, capability-token flow diagram, file map, framework-choice rationale"
      ],
      "inDevelopment": [
        "Daemon route /api/v1/auth/challenge — mobile fetches with synthetic fallback; needs gateway-side handler in K.2.5",
        "share.send / share.inbox / message.send / message.inbox routes — typed JSON-RPC bodies posted directly until @hypermesh/sdk enumerates them",
        "Dashboard NodeStatus shape uses inline casts — needs typed shape from @hypermesh/sdk types.ts (currently {status?, version?, block_height?})",
        "EAS submit metadata (ascAppId, appleTeamId, serviceAccountKeyPath) — eas.json templates ship with placeholders for foundation operator to fill"
      ],
      "planned": [
        "FALCON-1024 keygen + signing on device via UniFFI binding (hypermesh-ffi → C → JNI/Swift) — C.4.5",
        "ios/ + android/ native folders generated via `expo prebuild` — first build materialises them",
        "Real platform icons (assets/icon.png, splash.png, adaptive-icon.png) — placeholders gitignored",
        "Push notifications via APNs + FCM (replaces inbox polling) — post-C.4",
        "QR-code gateway pairing (camera permission already reserved in app.json) — C.5",
        "Biometric unlock for the capability token (TouchID/FaceID/fingerprint) before SDK calls",
        "Background sync for offline message queue — RN background tasks",
        "GitHub Actions EAS integration — operator-side only; signing keys must not live in CI secrets"
      ]
    },
    "completion": 63
  },
  {
    "id": "ngauge",
    "name": "NGauge",
    "description": "Work tracking, content receipts, capacity metrics, and network analytics for HyperMesh",
    "phase": "alpha",
    "features": {
      "working": [
        "Reed-Solomon sharding engine (ngauge::Sharder — encode/decode k-of-n via reed-solomon-erasure, last-shard length trailer, per-shard BLAKE3 integrity hash; byte-identical to the old blockmatrix impl; blockmatrix consumes it at its shard seam via pub use ngauge::sharding::Sharder)",
        "Adaptive ShardingConfig (adaptive_for_size delegates to lib ErasureCodingParams::for_asset_size — R14 RS param scaling; ngauge is now a MANDATORY blockmatrix dependency, intelligence feature gates only the analytics surface)",
        "Content receipts (BLAKE3 hash + timestamp + node signature, receipt bundles, verification)",
        "Metrics collector (compute cycles, bandwidth, latency, receipt density, activity scoring)",
        "KYC compliance checker (self-sovereign attestation structure, tier-based levels)",
        "Organic vs speculative traffic detection (pattern-based aggregate flow analysis)",
        "Governor throttle signal (activity score, band modifier, demurrage modifier, organic ratio)",
        "Capacity metrics (bytes served, compute delivered, storage, bandwidth, uptime)",
        "Multi-epoch capacity aggregation and trending (EpochTracker, CapacityTrend)",
        "Metrics streaming protocol (MetricsFrame, 4 payload types, encode/decode, privacy-mode-aware)",
        "Differential privacy filter (Laplace noise injection, epsilon-calibrated)",
        "Routing intelligence traits (TensorWeightModifier, PathPolicyRecommendation)",
        "Resource pool and lease contracts (sovereign allocation percentages, time-bounded leases)",
        "Lease manager (supply/demand matching, validation, lifecycle management)",
        "Pricing engine (Governor-adjusted, tier-aware multipliers)",
        "Content push manager (opt-in/out recipients, publisher registration, fee-based anti-spam)",
        "STOQ METRICS frame type integration (0xfe000007 custom frame)",
        "Swarm analytics data structures (ShardPopularity, demand maps, cascade tracker)",
        "Min-spec performance profiling (budget validation against R13 limits)",
        "Caesar in-transit/holding amount tracking (per-node economic state struct)",
        "STOQ-compatible API server (NGaugeStoqApi: 6 handlers, bind [::1]:9296)",
        "UDP metrics ingestion listener ([::1]:9297) — receives MetricsFrame datagrams, feeds into MetricsIngestionPipeline",
        "eBPF policy feedback — EbpfPolicyFeedback trait (apply_routing_rules + apply_privacy_action), RoutingIntelFeed auto-pushes privacy actions on publish_update()",
        "MetricsPublisher transport callbacks",
        "MetricsSubscriber byte ingestion",
        "RegionalAggregator periodic change detection",
        "Swarm analytics — windowed fetch tracking, popularity detection, ReplicationRecommendation (None/Replicate/UrgentReplicate)",
        "MetricsIngestionPipeline — configurable ingest pipeline for MetricsFrame processing (dedup, validation, backpressure)",
        "UI-facing API handlers — /throttle, /routing/advisory, /marketplace/pools, /marketplace/leases, /marketplace/pricing, /metrics/stream (response formats aligned with UI TypeScript interfaces via serde rename)",
        "Routing intelligence feedback consumed by blockmatrix EbpfFeedbackAdapter",
        "ReplicationTrigger consumed by blockmatrix swarm coordinator — ngauge popularity drives blockmatrix's 30s replication poll, which fetches extra replicas via TAG_SHARD_FETCH from providers in ShardLocationIndex",
        "Reconciler convergence closed (P6) — set_replica_count feedback hook (was the never-called gap) drives actual→desired replica convergence: after each fetch the actuator reports provider count back, ReplicationTrigger sees needed<=replicas and STOPS firing (swarm-emergent stable fixed point, verified by convergence tests)",
        "DispersionAdvisor (P6) — anti-affinity placement recommends where new replicas go instead of naive candidates[0]",
        "PeerTrustSignals + TrustBand — public reputation signals (activity_score, capacity, traffic_classification) exposed to trustchain federation gating; thresholds: composite activity ≥ 0.6, bandwidth ≥ 10 Mbps, uptime ≥ 0.9, organic-only traffic for Full"
      ],
      "inDevelopment": [
        "Collective intelligence aggregation — privacy-aware aggregation code exists but no multi-node data sources"
      ],
      "planned": [
        "Cross-node metrics federation via reflector pool"
      ]
    },
    "completion": 94
  },
  {
    "id": "stoq",
    "name": "STOQ Protocol",
    "description": "QUIC-based transport protocol with eBPF acceleration",
    "phase": "alpha",
    "features": {
      "working": [
        "QUIC/IPv6 transport via Quinn — connection pooling, health checks, adaptive congestion control",
        "Certificate management — two modes: Anonymous (ephemeral per-connection) and Authenticated (TrustChain-issued)",
        "Network isolation — PrivacyMode (Anonymous/Private/Public) with eBPF policy push",
        "eBPF-accelerated send/receive — AF_XDP zero-copy I/O via StoqEbpfTransport wrapper",
        "eBPF validation hooks — CertificateValidator + PacketValidator registered with hypermesh-ebpf",
        "Protocol extensions — packets, tokens, shards with canonical length-prefixed serialization",
        "Adaptive transport tiers — EWMA bandwidth estimation, MTU discovery, loss-based adjustment",
        "Multi-path QUIC — scope/privacy/federation policy enforcement, bandwidth-weighted scheduling",
        "Reflector pool transport — heartbeat/health tracking, quorum detection, sync protocol",
        "Post-quantum QUIC tunnel — X25519MLKEM768 hybrid key exchange via aws-lc-rs + rustls prefer-post-quantum",
        "BLAKE3 content hashing",
        "Real transport metrics (LatencyTracker with jitter, TransportSnapshot, per-connection stats)",
        "Identity-aware connection establishment (extract_node_id from X.509 SPKI)",
        "Certificate manager with real X.509 validation",
        "Certificate store persistence across restarts (~/.stoq/certs/)",
        "Graceful degradation with expired certificates (fallback to self-signed Anonymous)",
        "Permission enforcement at protocol layer (EKU-based scope checking)",
        "Service discovery registry (register/resolve/deregister by service type)",
        "Cipher suite negotiation for Private/Anonymous networks (R8)",
        "NGauge METRICS frame type (0xfe000007) — feature-gated handler",
        "MetricsFrame protocol wiring to ngauge",
        "Min-spec transport validation (R13 — bandwidth checks, connection budget)",
        "PoS fast validation — structural pre-checks on PoS tokens (field presence, size limits, format)",
        "Canonical PoS types consumed from lib (S1) — stoq/src/protocol re-exports hypermesh_lib::proof (SpaceProof/StakeProof/StateProof/TimeProof/WorkProof); the duplicate stoq-local PoS type set was deleted, and no magnitude (stake amount / difficulty / capacity) is checked at the transport gate",
        "FALCON-1024 PoS signature verification — pos_validator.rs verifies directly via pqcrypto_falcon::falcon1024 using issuer_pubkey from token, no TrustChain client needed",
        "MetricsFrameBridge eBPF integration — publish_ebpf_metrics() accepts HyperMeshMetrics, generates Congestion (drops/latency) + Routing (throughput/paths) frames",
        "Path scheduling feedback — PathSelector.apply_recommendation() accepts ngauge PathPolicyRecommendation, maps strategy to multi-path scheduler",
        "WAN transport (non-localhost bind, interface detection)",
        "MTU clamping for internet paths",
        "Connection migration scaffold",
        "NGauge→path scheduler feedback",
        "PosValidationState connection gate",
        "PathSelector recommendation polling",
        "Stale certificate auto-recovery — validates cert+key consistency on load, auto-deletes and regenerates on KeyMismatch",
        "Protocol-version negotiation (P6) — Msg1/Msg2 carry a protocol_version field (serde default STOQ_PROTOCOL_VERSION=1.0.0); peer_version_compatible rejects a major-version mismatch BEFORE any trust/application data, minor/patch tolerated, old peers omitting the field still handshake; mirrors blockmatrix::ipc::protocol version rules",
        "0x484D PoS extension-header emission (A4-CORRECT) — protocol layer prepends the plaintext WirePosHeader prefix (via hypermesh_ebpf::encode_pos_extension, magic 0x484D BE + WirePosHeader::from_pos_hash) ahead of the payload so XDP can PoS-classify at wire speed on the AF_XDP raw-send path; offset-pinned byte-identity tests vs the C hmesh_* structs"
      ],
      "inDevelopment": [
        "Bilateral handshake protocol — 3-message challenge-response, verified local two-node and local-to-GCP (trust.hypermesh.online), handshake metadata exchange for network_id",
        "Matrix-aware positioning — types exist from hypermesh_lib but not used in actual routing decisions"
      ],
      "planned": [
        "Real jitter benchmarking under controlled partitions",
        "Partition recovery timing measurements",
        "FALCON-1024 X.509 cert signing (requires upstream TLS spec for post-quantum signature algorithms in rustls/Quinn — key exchange already post-quantum via X25519MLKEM768)"
      ]
    },
    "completion": 88
  },
  {
    "id": "trustchain",
    "name": "TrustChain",
    "description": "Certificate Authority with Proof of State validation — currently single-node, target: decentralized",
    "phase": "alpha",
    "features": {
      "working": [
        "X.509 certificate issuance via rcgen (real CA-signed leaf certs, proper hierarchy)",
        "Certificate PEM encoding (rcgen .pem() output, proper base64)",
        "KeyUsage/EKU enforcement in issued certs (digital signature, key encipherment, server/client auth)",
        "Certificate Transparency (Merkle tree with consistency checker, inclusion proofs)",
        "Certificate rotation (CertificateRotationManager scans store, rotates expiring certs)",
        "Persistent revocation storage (JSON on disk, survives restart)",
        "OCSP responder structure",
        "CRL generator and distributor structure",
        "Workload identity types (NodeIdentity, ServiceIdentity, AgentIdentity)",
        "Identity-typed certificates (CertificateSubjectType: Node/Service/Agent)",
        "Cross-crate identity integration (AuthenticatedNode wraps lib::NodeId)",
        "TrustAssetKind includes Transmission variant (R10)",
        "StateAuthenticator (binary PoS authentication interface)",
        "FALCON-1024 node identity (FalconIdentity implements NodeSigner trait, keypair gen, persistence, BLAKE3 node ID — canonical implementation used by blockmatrix)",
        "Key rotation entries — KeyRotationEntry with FALCON-signed transition proof, old key signs new key authorization, rotation chain verification (genesis → current)",
        "Recovery passphrase commitment — HKDF-SHA512 + BLAKE3 deterministic commitment stored in genesis Identity asset, passphrase never persisted",
        "TrustChainProofProvider — implements StateProofProvider with FALCON-1024 signed WireSignedProof envelope (BLAKE3+FALCON signing/verification, replay-prevention nonce)",
        "PoS types consolidated into lib (S1) — trustchain no longer defines its own proof structs; proof_of_state re-exports hypermesh_lib's canonical set and attaches generation/crypto via the StateProofOps extension trait",
        "Stake-magnitude removed (S1) — no stake_amount, no minimum_stake/threshold, no PoW difficulty or nonce; PoStake is an identity binding (WHO) and PoWork is a work_hash (WHAT)",
        "FALCON-1024 identity signing — bilateral handshake proofs are FALCON-signed end-to-end (generate→sign→wire→verify), StakeProof covered by WireSignedProof envelope",
        "FALCON IS authentication — QUIC/TLS provides transport encryption only (AcceptAllVerifier bypasses cert verification), PoS bilateral handshake with FALCON-signed proofs is the sole authentication mechanism",
        "BLAKE3 content hashing",
        "Canonical ProofType from hypermesh-lib",
        "Production binary hardening (config file loading from TRUSTCHAIN_CONFIG env / ~/.hypermesh/trustchain.toml, graceful shutdown on SIGTERM/SIGINT, health endpoint, signal handling)",
        "Production DNS zone config for catalog.hypermesh.online and ngauge.hypermesh.online",
        "Real certificate operation metrics (CAMetrics atomic counters: issued, revoked, latency)",
        "Real PoS bilateral verification on peer connections",
        "PoS-gated CA certificate issuance",
        "CA federation with PoS-validated peers",
        "FALCON-1024 certificate signing in STOQ",
        "Threshold CA activation — split_ca_key() + sign_with_threshold() wire Shamir SSS into TrustChainCA",
        "FederationManager key share storage — store/get/remove/list KeyShares with fingerprint validation",
        "CRL propagation to federation — CrlDistributor.propagate_to_federation() pushes revocations to Full+Conditional peers; with FederationOcspTransport attached, actually delivers over wire",
        "OCSP federated check — federated_check() queries local store first; with FederationOcspTransport attached, polls Full/Conditional peers and returns Revoked on first hit, Unknown when all peers Unknown",
        "ThresholdSignCoordinator — broadcasts share-request via FederationTransport, awaits t-of-n responses with deadline, calls reconstruct_and_sign for FALCON-1024 signature",
        "TrustSignalProvider trait — pluggable ngauge-driven trust gating in FederationManager.add_peer_with_proof",
        "ngauge-gated TrustLevel demotion — peers with low ActivityScore/CapacityMetrics demoted to Conditional; ByzantineDetector flag forces Untrusted",
        "trustchain.request_cert IPC handler — routes through ThresholdSignCoordinator if federation in threshold mode, else local FALCON-1024 self-sign fallback",
        "OCSP HTTP/3 endpoint with federation fallback — POST /api/v1/trustchain/ocsp serves real OcspResponder; on local Unknown, falls back to FederationOcspTransport peer queries when attached",
        "CRL revocation auto-propagates to federation — CertificateStore.revoke_certificate calls CrlDistributor.propagate_to_federation when set_federation() context attached, alpha-default unchanged otherwise",
        "Cross-CA cert validation — FederationManager.is_federation_signed verifies FALCON-1024 cert blob against any Full/Conditional peer's public key, rejects unknown signers and Untrusted peers",
        "STOQ TAG_CRL_REQUEST/RESPONSE wire constants (0x33/0x34) and trustchain/crl/lookup STOQ API path — CrlLookupHandler exposes is_revoked + reason for federation peers",
        "Unsigned-proof fallback REMOVED — TrustChainProofProvider::validate_proof default-rejects any proof that is not a FALCON-signed WireSignedProof (the legacy raw-StateProof downgrade path is deleted, no structural-only accept)",
        "Signer↔identity binding — WireSignedProof::signer_matches / signer_pubkey_bytes let the bilateral handshake enforce that the proof signer is the same FALCON key that authenticated the peer identity (BLAKE3(pubkey)==node_id), closing the self-signed-proof Sybil vector",
        "localhost-testing removed from default features — release builds cannot enter StateProof::new_for_testing into real blocks; the feature is dev/test-only via resolver-2 dev-dependency scoping",
        "trustchain_ca gated behind required-features=[\\\"localhost-testing\\\"] — the self-signed auto-issue dev CA is excluded from release builds",
        "Dual-CA production split — trustchain-http3-server ([::1]:8444, gateway/clearnet CA) + trustchain-stoq-server ([::1]:8445, node-to-node CA), two production-clean binaries (zero new_for_testing), each loading a production State-bar [api] config; the production CA no longer runs the test-proof binary",
        "Device fingerprint folded into all four genesis proofs — DeviceFingerprint (machine-id/DMI/disk-serial/MAC via OsAbstraction) contributes to PoSpace/PoStake/PoWork/PoTime; three node-ids collapsed to one (BLAKE3(falcon_pubkey))",
        "Real PoTime clock witness — perform_ntp_sync returns a genuine wall-vs-monotonic drift reading with an epoch-window sanity check, not a hardcoded constant"
      ],
      "inDevelopment": [
        "Post-quantum TLS signatures — FALCON not usable for X.509 cert signing (TLS 1.3 only supports RSA/ECDSA/EdDSA in rustls). QUIC key exchange IS post-quantum via X25519MLKEM768 (aws-lc-rs)",
        "Kyber-1024 KEM integration — type exists but not used in certificate operations",
        "Certificate lifecycle tied to scope — scope-aware duration fields defined but not enforced in CA flow",
        "Scope-aware certificates — IdentityScopeExtension type defined but not embedded in issued certs",
        "Peer discovery (mDNS) — PeerAnnouncement struct exists, not tested multi-node",
        "STOQ-based API server — binds [::1]:8444, handler-to-cert-store wiring not fully verified",
        "HTTP/3 handlers — exist but real cert operation flow not tested end-to-end",
        "Byzantine detection — ByzantineDetector monitors local node only",
        "Let's Encrypt / ACME cert bootstrap for trust.hypermesh.online gateway TLS",
        "Inner four-proof depth — StakeProof/SpaceProof/WorkProof/TimeProof structural validity is identity/field presence + time bounds (S1 deleted every magnitude gate), NOT signature-verifying per proof; cryptographic authenticity is only the FALCON-1024 WireSignedProof envelope over the serialized StateProof. Per-proof attestation depth (real location/work-hash attestation) not yet enforced on hot paths",
        "Device-continuity enforcement is opt-in — verify_device_continuity rejects a copied identity on a different machine only under --require-hardware-auth (defaults off)"
      ],
      "planned": [
        "CRL distribution via blockchain (hash-linked blocks, delta CRLs)",
        "Cascading revocation across federated CAs",
        "Offline device grace period renewal",
        "Field device bootstrap with intermittent connectivity",
        "Recovery execution — passphrase-based key recovery, threshold peer attestation (k-of-n), Shamir key backup distribution (§6.2.3)",
        "Rotation chain verification wired into bilateral PoS handshake — peers verify key continuity on reconnect",
        "Identity distribution — key rotation entries propagated to peers via block sync, rotation alerts for theft detection (§6.2.4)"
      ]
    },
    "completion": 73
  },
  {
    "id": "ui",
    "name": "HyperMesh UI",
    "description": "React dashboard for HyperMesh ecosystem monitoring and management",
    "phase": "alpha",
    "features": {
      "working": [
        "React 19 + TypeScript 5.9 + Vite 6 + TailwindCSS 4 + Radix UI",
        "React Query state management (per-hook polling intervals, retry logic, cache invalidation)",
        "React Router v6 module routing with sub-navigation per module",
        "Accessibility (KeyboardNavigationProvider, ARIA labels, ScreenReaderOnly, LiveRegion)",
        "E2E tests (Playwright, 5 specs: accessibility, dashboard, modules, navigation, trustchain)",
        "Design tokens (lib/tokens.ts): moduleColors, statusColors, polling intervals",
        "Barrel exports (components/ui/index.ts): 40+ primitives, charts, composites, accessibility",
        "CSS custom properties (--hm-hypermesh through --hm-catalog) in oklch",
        "Custom SVG charts (LineChart, BarChart, PieChart, AreaChart, SparklineChart, GaugeChart — zero deps)",
        "ErrorBoundary: class component, module-aware error display + retry button",
        "ModuleLoading: skeleton-based loading placeholder",
        "blockmatrix-api.ts: 43 typed methods covering ALL IPC handlers (core, blockchain, dns, network, topology, asset, domain, dashboard, config, share, message, caesar, ngauge, trustchain, stoq, gateway, intelligence)",
        "useBlockMatrix.ts: 43 React Query hooks with per-hook polling intervals",
        "Zero mock data — errors shown as errors, not fake data",
        "HyperMeshOverview: node status, chain height, peers, assets, uptime",
        "BlockchainExplorer: block search, navigation, chain validation",
        "DnsManagement: record table, resolve lookup, register form",
        "TopologyView: node coordinate display, neighbor list with distances",
        "DomainManagement: domain list, register form, join with BLAKE3-HMAC token",
        "AssetManagement: asset list with content hashes, category badges",
        "StorePipeline: pipeline stages diagram (Compress→Encrypt→Shard→Distribute) + CLI reference",
        "NodeSettings: identity info (FALCON + Kyber), config viewer/filter, rotation",
        "ResourceConfiguration: config key search, full config table",
        "SharingManagement: share inbox, per-asset share button, connected peers",
        "StateProofDashboard: chain validation, four proof type reference, identity keys",
        "AdvancedAssetManagement: asset inventory from blockchain",
        "CaesarOverview: balance cards, staking, rewards, recent transactions (useCaesar* hooks)",
        "CaesarModule: Overview/Wallet/Rewards tabs",
        "NGaugeOverview: capacity metrics, traffic analysis, throttle status",
        "NGaugeAnalytics: capacity breakdown, traffic details, privacy sharing matrix",
        "NGaugeMarketplace: tier pricing with real capacity stats",
        "NGaugeRouting: routing advisory, congestion level, governor throttle",
        "IntelligenceStats: H1-H7 loop status, utilization, network flow",
        "SecurityMonitoringDashboard: CA status, cert health, federation peers, identity",
        "TrustChainRouting: node status, network management",
        "NetworkManagement: peers, domains, topology",
        "TransportDashboard: connection count, bytes transferred, per-connection details",
        "PerformanceView: throughput/latency/packet loss with grades",
        "DashboardMonitor: unified system monitor (Overview/Performance/Network tabs)",
        "PerformanceMonitor: STOQ transport metrics, active connections",
        "SystemStatusWidget: compact node health widget",
        "ShareDialog: share assets with peers by name/node ID",
        "InboxPanel: received share invitations with Accept/Reject + badge",
        "MessengerPanel: two-column messaging (conversation list + thread)",
        "ConversationList: peer list with last message preview",
        "MessageThread: bubble layout, auto-scroll, compose input",
        "Sidebar badges: live inbox + message unread counts from polling",
        "CatalogBrowse: asset search/filter from blockchain",
        "CatalogInstalled: registered assets with category badges",
        "CatalogCreate: asset creation form",
        "Tauri 2 desktop shell scaffolding (desktop/ — system tray, daemon subprocess manager, IPC bridge)",
        "First-run setup wizard (6 pages: welcome, privacy mode, identity, trustnet-test, foundation grant, done)",
        "Tray ↔ UpdateBanner bridge (update-available event from daemon's system.check_update IPC)",
        "Tauri bridge with graceful no-op fallback (UI builds standalone for Gateway use case)",
        "Single Gateway entry — UI paths match multi-service reverse-proxy architecture (M.1+M.1.5)",
        "TrustChain module — 9 sub-routes wired to real daemon X.509 data (M.2 + M.2.5)",
        "STOQ module — 4 sub-routes wired (M.3), polling gaps closed for chainValidation/topologyNeighbors/caesarTransactions, useDashboardList/useDashboardInfo hooks added",
        "Catalog deps + search + system check-update — UI wires (M.4) consuming real daemon handlers (M.4.5a/b/c)",
        "Test harness restored — tsc clean, vitest green, Playwright enumerable, legacy EnhancedCertificate chain retired (M.5)"
      ],
      "inDevelopment": [
        "Storybook stories — 3 existing, ~25 needed for full coverage",
        "Tauri desktop bundle — release.yml `desktop` matrix wired but continue-on-error (C.3.5: real icons, sidecar binary, platform polish)"
      ],
      "planned": [
        "Multi-node cluster topology 3D visualization",
        "Real-time SSE/WebSocket event stream (block announcements, message notifications)",
        "Tauri sidecar daemon binary (C.3.5 — currently relies on $PATH install)",
        "Windows named-pipe IPC for daemon ping (C.3.5 — currently Unix-socket only)"
      ]
    },
    "completion": 91
  }
];
